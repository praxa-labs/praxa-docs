---
title: "Lexically query one subject's candidates"
sidebarTitle: "Lexically query one subject's candidates"
description: "Requires a personal workspace key with memory:read. This endpoint is lexical only; it does not perform vector, semantic, or graph search."
api: "POST /v1/memory/query"
authMethod: "bearer"
playground: "interactive"
---

{/* Generated from the checked OpenAPI source. Run npm run docs:generate after changing the contract. */}

Requires a personal workspace key with memory:read. This endpoint is lexical only; it does not perform vector, semantic, or graph search.

<Info>
  **Availability:** Qualification preview. **Required scope:** `memory:read`.
</Info>

Use a disposable personal workspace key and verify the returned resource or receipt before treating the operation as complete.

## Request fields

<ParamField body="apiVersion" type="v1" required>
  apiVersion request field.
</ParamField>

<ParamField body="subject" type="string" required>
  subject request field.
</ParamField>

<ParamField body="query" type="string" required>
  query request field.
</ParamField>

<ParamField body="providerIds" type="array<mem0 | zep | graphiti | langgraph | letta | openai_agents | custom>">
  providerIds request field.
</ParamField>

<ParamField body="kinds" type="array<message | fact | summary | episode | pinned_context | document | entity | edge>">
  kinds request field.
</ParamField>

<ParamField body="limit" type="integer">
  limit request field.
</ParamField>

## Successful response

**200** — Bounded lexical matches for the requested subject.

<ResponseField name="apiVersion" type="v1" required>
  apiVersion response field.
</ResponseField>

<ResponseField name="retrievalMode" type="lexical" required>
  retrievalMode response field.
</ResponseField>

<ResponseField name="data" type="array<object>" required>
  data response field.
</ResponseField>

<RequestExample>
~~~bash cURL
curl --fail-with-body -X POST 'https://api.praxa.io/v1/memory/query' \
  -H "Authorization: Bearer $PRAXA_API_KEY" \
  -H "Content-Type: application/json" \
  --data '{
  "apiVersion": "v1",
  "subject": "customer-42",
  "query": "weekly summaries",
  "providerIds": [
    "custom"
  ],
  "kinds": [
    "fact"
  ],
  "limit": 20
}'
~~~
</RequestExample>

<ResponseExample>
~~~json 200
{
  "apiVersion": "v1",
  "retrievalMode": "lexical",
  "data": [
    {
      "lexicalRank": 0,
      "candidate": {
        "id": "018f0000-0000-7000-8000-000000000001",
        "state": "candidate",
        "contentDigest": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
        "createdAt": "2026-08-13T12:00:00.000Z",
        "record": {
          "apiVersion": "v1",
          "providerId": "mem0",
          "sourceId": "example",
          "externalRecordId": "example",
          "revision": "example",
          "kind": "message",
          "subject": "example",
          "visibility": "subject",
          "content": "example",
          "provenance": "example",
          "occurredAt": "example",
          "observedAt": "example"
        }
      }
    }
  ]
}
~~~
</ResponseExample>

## Verify and recover

- Treat `401`, `403`, and `409` as authority or state signals, not generic retry prompts.
- For `429` or retryable 5xx responses, follow server retry guidance and keep a bounded attempt budget.
- Revoke the disposable key and remove test data after validation.
